name: First Workflow

on: workflow_dispatch # manually trigger

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "First output"
          echo "Second output"
      - name: Print Goodbye
        run: echo "Done, bye!"
